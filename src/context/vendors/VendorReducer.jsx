
import {

    ADD_VENDOR,
    UPD_VENDOR,
    DLT_VENDOR,
    LST_VENDOR

} from '../../types/VendorTypes';


export default (state, action) => {

    switch(action.type) {

        case ADD_VENDOR:
            return {

            };

        case UPD_VENDOR:
            return {

            };

        case DLT_VENDOR:
            return {

            };

        case LST_VENDOR:
            return {

            };

        default:
            return state;
    }


}